[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"
  VITE_API_URL = "https://laravel-postly-api.onrender.com"

# Vue Router history mode — send all routes to index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
